export type Vertex = readonly [number, number, number];

/**
 * CommodityType enum - defines the types of commodities available
 */
export enum CommodityType {
  COAL = "coal",
  OIL = "oil",
  GAS = "gas",
  URANIUM = "uranium",
}